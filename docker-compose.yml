version: "3.8"

services:
  mysql:
    image: mysql:8.0
    container_name: dorm_management_mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: hostel
      MYSQL_ROOT_PASSWORD: 
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
